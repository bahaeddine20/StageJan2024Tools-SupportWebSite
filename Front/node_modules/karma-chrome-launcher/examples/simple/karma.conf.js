module.exports = function (config) {
  config.set({
    basePath: '',
    frameworks: ['mocha'],
    files: [
      // Adjust the file pattern to match your test files
      '*.js'
    ],
    exclude: [],
    reporters: ['progress', 'junit'],
    junitReporter: {
      outputDir: 'junit-front-reports', // directory for JUnit XML files
      outputFile: 'test-results.xml' // name of the XML file
    },
    port: 9876,
    colors: true,
    logLevel: config.LOG_INFO,
    autoWatch: false,
    browsers: ['Chrome'],
    singleRun: false,
    plugins: [
      // You don't need to require karma and karma-mocha here
      // 'karma',
      // 'karma-mocha',
      'karma-junit-reporter'
    ]
  })
}
