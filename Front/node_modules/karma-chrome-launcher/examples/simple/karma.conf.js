var karmaMocha = require('karma-mocha');
var karmaJUnitReporter = require('karma-junit-reporter');
var karmaChromeLauncher = require('karma-chrome-launcher');

module.exports = function (config) {
  config.set({
    basePath: '',
    frameworks: ['mocha'],
    files: [
      '*.js'
    ],
    exclude: [],
    reporters: ['progress', 'junit'],
    junitReporter: {
      outputDir: 'junit-front-reports', // directory for JUnit XML files
      outputFile: 'test-results.xml' // name of the XML file
    },
    port: 9876,
    colors: true,
    logLevel: config.LOG_INFO,
    autoWatch: true,
    browsers: ['Chrome'],
    singleRun: true,
    plugins: [
      karmaMocha,
      karmaJUnitReporter,
      karmaChromeLauncher // Add any other plugins you're using
    ]
  });
};
