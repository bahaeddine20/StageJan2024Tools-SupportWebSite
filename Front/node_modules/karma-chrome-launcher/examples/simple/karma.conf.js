module.exports = function (config) {
  config.set({
    basePath: '',

    frameworks: ['mocha'],

    files: [
      '*.js',
      Front/src/app/crud/services/empcrud.service.spec.ts ,
      Front/src/app/crud/services/team-service.service.spec.ts,
      Front/src/app/crud/team-add-edit/team-add-edit.component.spec.ts,

    ],

    exclude: [],

    reporters: ['progress', 'junit'],
    junitReporter: {
      outputDir: 'junit_reports/', // Output directory for XML reports
      outputFile: 'front-test-results.xml' // Name of the XML report file
    },

    port: 9876,

    colors: true,

    logLevel: config.LOG_INFO,

    autoWatch: false,

    browsers: ['Chrome'],

    singleRun: false,

    plugins: [
      require('../../'),
      'karma-mocha'
    ]
  })
}
