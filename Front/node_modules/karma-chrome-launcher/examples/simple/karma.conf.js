const jasmine = require("jasmine");
module.exports = function (config) {
  config.set({
    basePath: '',
    frameworks: ['jasmine'],
    files: [
      // Adjust the file pattern to match your test files
      '*.ts'
    ],
    exclude: [],
    reporters: ['progress', 'junit'],

    port: 9876,
    colors: true,
    logLevel: config.LOG_INFO,
    autoWatch: false,
    browsers: ['Chrome'],
    singleRun: false,
    plugins: [
      'karma-jasmine',
      'karma-junit-reporter'
    ],
    junitReporter: {
      outputDir: '', // directory for JUnit XML files
      outputFile: 'junit-front-reports/test-results.xml' // name of the XML file
    },
  })
}
